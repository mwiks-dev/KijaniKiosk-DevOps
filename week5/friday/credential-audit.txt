KijaniKiosk Credential Audit
============================
Date: 21 Agosti 2026 06:08:48 alasiri EAT

CHECK 1 — Jenkinsfile credential scan
--------------------------------------
128:                withCredentials([usernamePassword(
131:                    passwordVariable: 'NEXUS_PASS'
140:                        NEXUS_TOKEN=$(printf '%s:%s' "$NEXUS_USER" "$NEXUS_PASS" \
145:        //$NEXUS_HOST/:_auth=$NEXUS_TOKEN

Expected: credential references only; no literal passwords, tokens or secrets.

CHECK 2 — Complete Jenkinsfile Git history
--------------------------------------------
-                            passwordVariable: 'NEXUS_PASS'
-                            NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
+                    passwordVariable: 'NEXUS_PASS'
+                        NEXUS_TOKEN=$(printf '%s:%s' "$NEXUS_USER" "$NEXUS_PASS" \
-                    passwordVariable: 'NEXUS_PASS'
-                        NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
+                            passwordVariable: 'NEXUS_PASS'
+                            NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
+                    passwordVariable: 'NEXUS_PASS'
-                            passwordVariable: 'NEXUS_PASS'
-                            NEXUS_TOKEN=$(printf '%s:%s' \
-                                "${NEXUS_PASS}" | base64 | tr -d '\\n')
+                        NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
-                        passwordVariable: 'NEXUS_PASS'
+                            passwordVariable: 'NEXUS_PASS'
-                            NEXUS_TOKEN=$(echo -n "${NEXUS_USER}:${NEXUS_PASS}" | base64)
+                            NEXUS_TOKEN=$(printf '%s:%s' \
+                                "${NEXUS_PASS}" | base64 | tr -d '\\n')
+                        passwordVariable: 'NEXUS_PASS'
+                            NEXUS_TOKEN=$(echo -n "${NEXUS_USER}:${NEXUS_PASS}" | base64)

Expected: no literal credential values anywhere in Jenkinsfile history.

CHECK 2B — Broader credential pattern scan
--------------------------------------------
-                            passwordVariable: 'NEXUS_PASS'
-                            NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
+                    passwordVariable: 'NEXUS_PASS'
+                        NEXUS_TOKEN=$(printf '%s:%s' "$NEXUS_USER" "$NEXUS_PASS" \
-                    passwordVariable: 'NEXUS_PASS'
-                        NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
+                            passwordVariable: 'NEXUS_PASS'
+                            NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
+                    passwordVariable: 'NEXUS_PASS'
-                            passwordVariable: 'NEXUS_PASS'
-                            NEXUS_TOKEN=$(printf '%s:%s' \
+                        NEXUS_TOKEN=$(printf "%s:%s" "$NEXUS_USER" "$NEXUS_PASS" | base64 | tr -d '\\n')
-                        passwordVariable: 'NEXUS_PASS'
+                            passwordVariable: 'NEXUS_PASS'
-                            NEXUS_TOKEN=$(echo -n "${NEXUS_USER}:${NEXUS_PASS}" | base64)
+                            NEXUS_TOKEN=$(printf '%s:%s' \
+                        passwordVariable: 'NEXUS_PASS'
+                            NEXUS_TOKEN=$(echo -n "${NEXUS_USER}:${NEXUS_PASS}" | base64)

CHECK 3 — Jenkins build logs
----------------------------
Reviewed Jenkins console output for the relevant pipeline runs.
Credential variables were masked and no plaintext Nexus password/token was observed.

CHECK 4 — .npmrc Git history
----------------------------

Expected: no output; .npmrc was not committed.

CHECK 4B — Current Git tracking status
--------------------------------------

Expected: no output.

ADDITIONAL CHECK — Current repository secret scan
------------------------------------------------
./kijanikiosk-payment/node_modules/@types/node/crypto.d.ts:694:     * const password = 'Password used to generate key';
./kijanikiosk-payment/node_modules/@types/node/crypto.d.ts:738:     * const password = 'Password used to generate key';
./kijanikiosk-payment/node_modules/@types/node/crypto.d.ts:770:     * const password = 'Password used to generate key';
./kijanikiosk-payment/node_modules/@types/node/crypto.d.ts:938:     * const password = 'Password used to generate key';
./kijanikiosk-payment/node_modules/@types/node/crypto.d.ts:981:     * const password = 'Password used to generate key';
./kijanikiosk-payment/node_modules/@types/node/crypto.d.ts:1005:     * const password = 'Password used to generate key';
./kijanikiosk-payment/node_modules/esprima/dist/esprima.js:5065:	    DefaultRestParameter: 'Unexpected token =',
./kijanikiosk-payment/node_modules/esprima/dist/esprima.js:5113:	    UnexpectedToken: 'Unexpected token %0',
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:29:    startToken = "{|";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:30:    endToken = "|}";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:34:    startToken = "#{";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:35:    endToken = "}";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:51:    startToken = "[|";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:52:    endToken = "|]";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:54:    startToken = "#[";
./kijanikiosk-payment/node_modules/@babel/generator/lib/generators/deprecated.js:55:    endToken = "]";
./kijanikiosk-payment/node_modules/@babel/parser/lib/index.js:6080:      topicToken: "^^"
./kijanikiosk-payment/node_modules/@babel/parser/lib/index.js:6095:      topicToken: "@@"
./kijanikiosk-payment/node_modules/@babel/parser/lib/index.js:10696:        throw new Error(`Plugin conflict between \`["pipelineOperator", { proposal: "hack", topicToken: "#" }]\` and \`${JSON.stringify(["recordAndTuple", pluginsMap.get("recordAndTuple")])}\`.`);

Expected: no literal credentials.

